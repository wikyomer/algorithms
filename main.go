package main

import (
	"fmt"
)

func main() {
	fmt.Print(calculation("3 + 2 * 4"))
	//expression, err := bufio.NewReader(os.Stdin).ReadString('\n')
	//if err != nil {
	//	panic(err)
	//}

	//fmt.Printf("array: %v\n", expression)
}
